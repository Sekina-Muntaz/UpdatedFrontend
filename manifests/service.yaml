# Generated by Containerization Assist - AI powered configuration
# Kubernetes Service for DSS Frontend Vue.js Application

apiVersion: v1
kind: Service
metadata:
  name: dss-frontend-v2
  namespace: default
  labels:
    app: dss-frontend-v2
    version: v1
  annotations:
    description: "Service for DSS Frontend Vue.js Application"
spec:
  type: ClusterIP
  selector:
    app: dss-frontend-v2
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  sessionAffinity: None
---
# Generated by Containerization Assist - AI powered configuration
# Kubernetes Service for internal ClusterIP access

apiVersion: v1
kind: Service
metadata:
  name: dss-frontend-v2-internal
  namespace: default
  labels:
    app: dss-frontend-v2
    variant: internal
spec:
  type: ClusterIP
  clusterIP: None  # Headless service for direct pod access
  selector:
    app: dss-frontend-v2
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
