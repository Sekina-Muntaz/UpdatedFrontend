# Generated by Containerization Assist - AI powered configuration
# Kubernetes ConfigMap for DSS Frontend Vue.js Application

apiVersion: v1
kind: ConfigMap
metadata:
  name: dss-frontend-config
  namespace: default
  labels:
    app: dss-frontend-v2
    version: v1
data:
  # Application configuration
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  
  # API configuration - update these with your actual values
  VITE_API_URL: "https://api.example.com"
  VITE_AUTH_API_URL: "https://auth.example.com"
  
  # Application settings
  VITE_APP_NAME: "DSS Frontend"
  VITE_APP_VERSION: "1.0.0"
  
  # Feature flags
  VITE_ENABLE_ANALYTICS: "true"
  VITE_ENABLE_SENTRY: "false"
  
  # Session configuration
  SESSION_TIMEOUT: "1800"  # 30 minutes in seconds
  SESSION_WARNING_TIME: "1500"  # 25 minutes
---
# Generated by Containerization Assist - AI powered configuration
# Kubernetes Secret for sensitive data (base64 encoded in production)
# Note: In production, use External Secrets Operator or Azure Key Vault integration

apiVersion: v1
kind: Secret
metadata:
  name: dss-frontend-secrets
  namespace: default
  labels:
    app: dss-frontend-v2
    version: v1
type: Opaque
stringData:
  # API keys and tokens - replace with actual values
  API_KEY: ""
  AUTH_TOKEN: ""
  SENTRY_DSN: ""
